export enum SystemRoles {
  USERS = 'realm:sys_dashpesq-users',
  ADMIN = 'realm:sys_dashpesq-admin',
}

export enum Status {
  PROGRESS = 'In Progress',
  PENDING = 'Pending',
  CONCLUDED = 'Concluded',
  NOT_IMPORTED = 'Not Imported',
  LOADING = 'Loading',
}
